---
  一、当前架构的本质与瓶颈

  当前流水线本质上是 Prompt Chain：每个阶段是一次（或带重试的）LLM 调用，前一阶段的文本输出作为后一阶段的输入。这带来的瓶颈：

  ┌────────────────┬───────────────────────────────────────────────────────────────┐
  │      瓶颈      │                             表现                              │
  ├────────────────┼───────────────────────────────────────────────────────────────┤
  │ 单次推理无纠错 │ Extractor 一次调用决定了全部 claims，错了没法自我修正         │
  ├────────────────┼───────────────────────────────────────────────────────────────┤
  │ 无外部知识     │ 分析局限时只能靠模型内部知识，无法查相关文献或 benchmark 数据 │
  ├────────────────┼───────────────────────────────────────────────────────────────┤
  │ 验证粗糙       │ toy script 生成→执行→对照 是单向的，失败后不会反思改进        │
  ├────────────────┼───────────────────────────────────────────────────────────────┤
  │ claim 粒度失控 │ 模型倾向于提取"指标提升了 X%"这种容易写但信息密度低的 claim   │
  └────────────────┴───────────────────────────────────────────────────────────────┘

  ---
  二、可以演进的方向

  短期（在 prompt 层面立刻可做）

  1. Claim 结构从"一句话"改为"论证链"

  这就是你提的方向。当前 Claim 只有一个 statement 字段，模型自然倾向于填"X 提升了 Y%"。如果强制拆成因果链，压缩劣质 claim 的生存空间：

  class Claim(BaseModel):
      problem: str        # 针对什么问题
      method_applied: str  # 应用了什么方法
      mechanism: str       # 方法为什么有效（因果逻辑）
      result: str          # 产生了什么效果（量化或定性）
      condition: str       # 在什么条件下成立
      type: ClaimType
      confidence: float

  每个字段限 1-2 句话。这样模型被迫填满因果链，"纯指标"claim 会因 mechanism 为空被过滤。

  2. 分层提取 — 先骨架后细节

  当前是一次性全量提取（最多 5 方法 + 6 观点），模型注意力分散。可以改为：

  Pass 1: 提取方法骨架（方法名 + 一句话概述 + 步骤列表）
  Pass 2: 对每个方法，提取其支撑的实验主张
  Pass 3: 对每个主张，展开前提假设 + 局限性自检

  每一步的 prompt 更聚焦，输出质量会显著提高。

  3. Claim 去重与合并

  5 个方法各自可能产出 2-3 条 claim，结果 6 条里可能有 3 条本质是同一件事（"软总线更快"、"hmdfs
  传输开销低"、"分布式推理近线性加速"其实都在论证同一个架构优势）。加一个 LLM 合并步骤，输出 3-4 条互不重叠的核心主张。

  中期（引入 agentic 模式，需要改编排）

  4. ReAct 式迭代提取

  Extractor 先列出一份"候选主张"草稿（不必填全字段）
    → 对每条候选，反问：
      "这个主张的前提假设是什么？原文哪里支持？"
      "有没有反例或边界条件？"
      "原文的实验设计是否真的能支撑这个结论？"
    → 根据回答修正 / 降级 / 删除

  编排层从线性 DAG 变成带条件分支的循环图。

  5. 多 reviewer 辩论

  Reviewer A（乐观审稿人）: 提取 claims + 支撑证据
  Reviewer B（批判审稿人）: 对每条 claim 找反例、挑逻辑漏洞
  Reviewer C（方法专家）: 判断论证链是否严密
  Judge: 综合三方意见给出最终 claim 列表 + 置信度

  其实当前已有提取→验证→分析的雏形，但各阶段之间是流水线而非对抗关系。

  6. 外部知识锚定

  当前分析局限性时，模型只能靠训练数据中的记忆。可以加入：

  - Semantic Scholar / arXiv API：检索相关论文，对比该方向 SOTA 指标，自动发现"声称 SOTA 但 baseline 选了弱版本"这类问题
  - PapersWithCode：查 benchmark leaderboard，验证报告的数字是否合理
  - 引用检查：论文声称引用的工作是否真的支持其论点（检查引用链）

  长期（需要显著改动）

  7. 可复现性逐级验证

  当前是生成一个 toy script 就完了。可以做成层次化：

  Level 1: 伪代码逻辑走查（检查算法步骤是否有循环依赖、死锁等）
  Level 2: toy 数值模拟（当前做的事）
  Level 3: 合成数据 + 最小实现（如果真的要做）
  Level 4: 公开数据集 + 完整复现 pipeline

  Level 1 不需要沙箱，速度快，可以过滤掉大部分逻辑错误。

  8. 局限性分析的因果归因

  当前是"列出这篇论文的局限"，但每条局限没有和具体 claim 建立因果链。理想的输出是：

  Claim: "分布式推理延迟仅比本地高 4.09%"
    └── Limitation: 实验仅用 100 条文本、未报告方差
         └── Impact: 4.09% 这个数字的 95% CI 可能很宽，无法排除 20%+ 退化
         └── Suggested fix: 报告多组数据的标准差 + 做 bootstrap CI

  即每条局限必须回答"如果不修，最坏会导致什么后果"。

  ---
  三、关于 Claim 的因果链结构

  你说的"针对什么问题 → 应用了什么方法 → 解决了什么问题 → 产生了什么指标"本质上是把 claim
  从断言（assertion）升格为论证（argument）。建议修改后的 Claim 模型大致是：

  class Claim(BaseModel):
      """一条有因果链的学术主张."""
      id: str
      type: ClaimType  # theoretical | empirical | comparative | design

      # 因果链
      problem: str          # 要解决的问题 / 观察到的现象
      method: str           # 采用的方法 / 机制
      mechanism: str        # 为什么方法有效（因果逻辑，这是核心）
      evidence: str         # 支撑证据（实验数据 / 数学推导 / 定性分析）
      condition: str        # 在什么前提下成立

      # 元信息
      confidence: float
      assumptions: list[str]
      source_sections: list[str]  # 原文依据

  对应的提取 prompt
  也应该从"列出核心观点"改为"对每条观点，补全因果链的五个环节"。如果某个环节原文没有明确给出，就标记为缺失——这本身就会成为一条局限性。